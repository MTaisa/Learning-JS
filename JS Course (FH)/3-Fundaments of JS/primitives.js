// PRIMITIVES
// Primitives are data that is not and object and is inmutable
// There are six types of primitive data 

// Booleans
const b1 = true;

// Null 
const nulo = null;

// undefined
const data = undefined;

// Number (This includes integers and floats)
const n = 24;

// Strings
const chain = 'Hello World';

// Symbol (It is a unique value that is not equal to any other value)
let symbol1 =Symbol('a');
let symbol2 =Symbol('a');

console.log(typeof symbol1);
console.log(typeof symbol2);
console.log(symbol1 === symbol2);


