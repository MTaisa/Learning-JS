let a = 10,
    b = 20,
    c = 30,
    d = 40;

// This is one form to show the name of the varible and its value
console.log("a", a);
console.log("b", b);
console.log("c", c);

// This is a coolest way to do it
console.log({a}); // The curly braces indicate it is an object
console.log({b});
console.log({c});

// Console.table

console.table({a, b, c, d})

console.warn("This is just a warning");
console.error("Well....there is an error");

// BREAKPOINTS AND DEBUGGING

// Basically a breakpoint is a point where the program stops and shows everything that has happened
